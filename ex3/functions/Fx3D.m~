function [ output_args ] = Fx3D( V, N, F, sigma, debug )
    %FX3D calculate F(x) = C0(x) for 3D point x
    
    % V     vertex points (samples) 
    % 
    
    C0 = CalC0(F, V, N, sigma, debug);
    
end

